<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
  <br><br>
  This will be fine in one-depth block with haml.
  haml cells: <%= cell(:post_h).call(:link_good, :post => @post) %>
  <br><br>
  This also works fine in two-depth block with haml.
  haml cells: <%= cell(:post_h).call(:link_2d, :post => @post) %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
